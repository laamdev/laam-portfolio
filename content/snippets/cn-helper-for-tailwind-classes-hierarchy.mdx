---
title: cn helper for tailwind classes hierarchy
publishedAt: 2023-08-03
summary: xxx.
---

```jsx

export default function Button({className, ...props}: ButtonProps) {
    const [pending, setPending] = useState(false)
    const [loading, setLoading] = useState(false)

    return(
        <button className={cn(
            "bg-blue-500 py-2 px-4", // tw string
            className, // prop
            loading && "bg-red-500", // logical AND contional
            {
                "bg-gray-500": pending // object syntax conditional
            })} {...props}>
            {`Submit`}
        </button>
    )
}
```
